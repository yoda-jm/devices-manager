server.port=8082
spring.application.name=device-viewer

# Actuator (device-viewer is an internal UI tool)
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never
