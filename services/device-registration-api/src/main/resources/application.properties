server.port=8081
spring.application.name=device-registration-api

# Actuator security: only expose health endpoint
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never
